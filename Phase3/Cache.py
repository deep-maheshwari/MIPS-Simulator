class Cache:

    block_size = 0
    set_assoc = 0
    blocks = 0
    miss_penalty = 0 #time to fetch_from cache to it's next level
    miss_count = 0
    cache_lat = 0 #time taken to fetch from cache
    hit_count = 0
    cache = {}

    def __init__(self,block_size,set_assoc,blocks):
        self.block_size = block_size
        self.blocks = blocks
        self.set_assoc = set_assoc

        for i in range(blocks):
            

    def Cache_controller(index):
    
    index = str(bin(index))
    # 1098th {'index':index[-2:], 'offset':1098%block_size , 'tag':'038080392' } 

    def search():
        miss_count+=1
        fetch_block()
        return{}
    
    def place_block():
        num = 1096
        temp = []
        count = 0
        while(count<4):
            if(num+count>len(data['.word'])):
                temp.append('')
            else:
                temp.append(data['.word'][num+count])
        if(#already block is there):
            replace(tag,policy)
        else:
            cache['set index']['tag'] = temp

    